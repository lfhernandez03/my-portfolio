---
const { title, text, href, src } = Astro.props;
---

<div class="flex items-center space-x-2">
  <div class="flex items-center justify-center w-14 h-14 rounded-full bg">
    <img src={src} alt="{src}" class="w-12 h-12 object-cover object-center" />
  </div>
  <div class="flex flex-col">
    <h2 class="text-lg">{title}</h2>
    <a href={href}>{text}</a>
  </div>
</div>

<style>
  .bg {
    background-color: rgba(255, 255, 255, 0.1); /* Fondo blanco translúcido */
  }
</style>
